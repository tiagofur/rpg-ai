# Contribuir

- Lee docs/GDD.md y docs/MVP_SPEC.md antes de trabajar en features.
- Usa ramas `feature/<nombre>` y PRs hacia `dev` (o `main` si prefieres).
- Añade tests cuando implementes lógica de resolución de acciones.
- Para cambios en prompts: actualiza system_prompts/ y añade tests de ejemplo (reproducibilidad).